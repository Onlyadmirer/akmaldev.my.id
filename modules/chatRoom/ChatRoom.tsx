"use client";

// import { useTest } from "@/lib/fetch";

function ChatRoom() {
  // const { data, isLoading, isError } = useTest();

  return (
    <div>
      <h1>will update</h1>
    </div>
  );
}

export default ChatRoom;
